using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DamageManager : MonoBehaviour
{
    //Here we pull the stats from both the objects involved in the attack acction and the calculate the damage returned after resistance and all that are calculated
    public float CalculateDamage()
    {
        //In here we get the objects and thiers stats as well as thier defence amount
        //We then calculate the damage total and retun it
        return -1;
    }
}
